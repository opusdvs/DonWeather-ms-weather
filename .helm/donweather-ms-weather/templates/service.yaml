apiVersion: v1
kind: Service
metadata:
  name: {{ include "donweather-ms-weather.fullname" . }}
  labels:
    {{- include "donweather-ms-weather.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "donweather-ms-weather.selectorLabels" . | nindent 4 }}
