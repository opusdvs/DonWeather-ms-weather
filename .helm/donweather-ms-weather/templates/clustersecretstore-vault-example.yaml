# Пример ClusterSecretStore для Vault
# Раскомментируйте и настройте под вашу инфраструктуру
# Примените: kubectl apply -f .helm/donweather-ms-weather/templates/clustersecretstore-vault-example.yaml

# apiVersion: external-secrets.io/v1
# kind: ClusterSecretStore
# metadata:
#   name: vault-backend
# spec:
#   provider:
#     vault:
#       server: "https://vault.example.com:8200"
#       path: "secret"  # Путь к KV секретам (для KV v2)
#       version: "v2"   # v1 или v2
#       auth:
#         # Вариант 1: Kubernetes Service Account
#         kubernetes:
#           mountPath: "kubernetes"
#           role: "external-secrets-operator"
#           serviceAccountRef:
#             name: "external-secrets-operator"
#         # Вариант 2: Token
#         # tokenSecretRef:
#         #   name: "vault-token"
#         #   key: "token"
#         # Вариант 3: AppRole
#         # appRole:
#         #   path: "approle"
#         #   roleId:
#         #     value: "your-role-id"
#         #   secretRef:
#         #     name: "vault-approle-secret"
#         #     key: "secret-id"
